const Navbar = () => {
    return (
        <nav className="fixed top-0 w-full bg-white border-b">
            <div className="h-14 w-full max-w-screen-lg mx-auto px-4 flex items-center justify-between">

            </div>
        </nav>
    );
};

export default Navbar;